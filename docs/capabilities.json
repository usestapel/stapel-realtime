{
  "module": "stapel-realtime",
  "version": "0.1.3",
  "provides": "Realtime delivery substrate: the L1 library behind the Signal primitive (stapel_core.comm.signal). Ships the Channels/Redis transport for the core's signal-delivery seam, the two consumers every browser socket in the fleet is built from (EphemeralStreamConsumer for at-most-once Signal fan-out; ResumableStreamConsumer for hello/welcome/replay/live journals with seq dedup and a bounded replay window), the versioned v1 wire envelope, the canonical <mod>:<scope_type>:<scope_id>[:<topic>] stream key, a fail-closed per-stream authorize seam with the workspace-capability authorizer, revoke-to-kick, heartbeat with JWT-exp re-check, disconnect-on-overflow backpressure, the fleet close-code canon, build_websocket_application() host assembly with a port-aware origin guard, and five system checks. No models, migrations, views, urls or comm surface of its own; it is installed as a Django app only so its checks are registered.",
  "axes": [
    {
      "key": "HEARTBEAT_S",
      "kind": "int",
      "default": 25,
      "group": "realtime.liveness",
      "gates": {
        "operations": [],
        "co_gates": []
      },
      "curated": {
        "summary": "Seconds between server-initiated ping frames. Each tick ALSO re-checks the JWT's exp and closes 4401 if it has passed, so setting this to 0 disables two things, not one (realtime.W003 says so at check time).",
        "business_label": "How often a live socket is proven alive and still credentialed"
      }
    },
    {
      "key": "HEARTBEAT_TIMEOUT_S",
      "kind": "int",
      "default": 10,
      "group": "realtime.liveness",
      "gates": {
        "operations": [],
        "co_gates": []
      },
      "curated": {
        "summary": "Seconds to wait for the client's pong before closing 4408.",
        "business_label": "Grace period before a silent socket is reaped"
      }
    },
    {
      "key": "MAX_REPLAY",
      "kind": "int",
      "default": 500,
      "group": "realtime.resumable",
      "gates": {
        "operations": [],
        "co_gates": []
      },
      "curated": {
        "summary": "Widest resume gap replayed inline on a resumable stream, and the LIMIT passed to the module's get_replay_rows hook. A wider gap answers error{code=resync} and the client re-hydrates over HTTP pagination — there is no infinite rewind by design.",
        "business_label": "How far back a reconnecting client is caught up over the socket"
      }
    },
    {
      "key": "SEND_QUEUE_SIZE",
      "kind": "int",
      "default": 100,
      "group": "realtime.backpressure",
      "gates": {
        "operations": [],
        "co_gates": []
      },
      "curated": {
        "summary": "Outbound frames buffered per socket before the substrate closes 4413. The producer never waits on a slow client; the client resyncs on reconnect.",
        "business_label": "How far a slow browser may fall behind before it is dropped"
      }
    },
    {
      "key": "AUTHORIZE_CACHE_S",
      "kind": "int",
      "default": 30,
      "group": "realtime.authorization",
      "gates": {
        "operations": [],
        "co_gates": []
      },
      "curated": {
        "summary": "Seconds an authorize() verdict is reused for the same (user, stream) within one socket, matching the capability cache the HTTP path already accepts. This is the acknowledged ceiling on the residual leak window; the way to shorten it for a specific stream is revoke(), not a lower TTL.",
        "business_label": "How long a subscription verdict is trusted before it is re-asked"
      }
    },
    {
      "key": "ALLOWED_ORIGINS",
      "kind": "list",
      "default": [],
      "group": "realtime.host",
      "gates": {
        "operations": [],
        "co_gates": []
      },
      "curated": {
        "summary": "Exact origins WITH port that may open a socket. Empty disables the guard (realtime.W002); an entry that is not a scheme://host[:port] origin is realtime.E003, because a guard that can never match is worse than no guard. Configured-but-all-malformed refuses everything rather than falling open.",
        "business_label": "Which web origins may open a realtime socket"
      }
    },
    {
      "key": "URL_PREFIX",
      "kind": "str",
      "default": "ws",
      "group": "realtime.host",
      "gates": {
        "operations": [],
        "co_gates": []
      },
      "curated": {
        "summary": "The edge convention every module's socket routes live under (/ws/<mod>/...), asserted by realtime.W004 so the generated route table stays a projection of the manifests.",
        "business_label": "URL prefix for socket routes"
      }
    },
    {
      "key": "LAYER_SOCKET_TIMEOUT_MIN",
      "kind": "int|null",
      "default": null,
      "group": "realtime.layer",
      "gates": {
        "operations": [],
        "co_gates": []
      },
      "curated": {
        "summary": "Minimum acceptable redis socket_timeout for the channel layer; null derives it from the layer's expiry + 10s. redis-py >= 8 defaults socket_timeout to 5s, which tears down a consumer parked in BZPOPMIN on an idle stream — realtime.E002.",
        "business_label": "Floor for the channel layer's redis socket timeout"
      }
    }
  ],
  "extension_points": [
    {
      "name": "authorize()",
      "kind": "required_hook",
      "summary": "The per-stream authorization seam on every consumer. FAIL-CLOSED: a subclass that does not override it (or set `authorizer`) subscribes nobody. Set `authorizer = WorkspaceCapability('<mod>.read')` for a ws-scoped stream, or write an async (scope, stream_key) -> bool of your own (MODULE.md 'Extension points')."
    },
    {
      "name": "EphemeralStreamConsumer",
      "kind": "base_class",
      "summary": "Subclass for Signal delivery: set module/scope_type/stream_key_kwarg (or override get_stream_key) plus an authorizer, and the socket is done. No hooks required — there is no journal."
    },
    {
      "name": "ResumableStreamConsumer",
      "kind": "base_class",
      "summary": "Subclass for a journal with catch-up. Two async hooks: get_server_seq() and get_replay_rows(after_seq, limit) -> Sequence[JournalRow]. The welcome, the bounded replay, the resync verdict and the replay/live dedup are the base class's."
    },
    {
      "name": "ChannelsSignalTransport",
      "kind": "transport",
      "summary": "The v1 signal-delivery backend the core's STAPEL_COMM['SIGNAL_TRANSPORT'] axis names. Callable and .send()-able, so either resolution convention in the core works. Swap it for a dotted path to your own object to move Signal onto a different bus."
    },
    {
      "name": "OriginGuard",
      "kind": "middleware",
      "summary": "The ASGI origin allowlist build_websocket_application() installs. Instantiable directly with explicit `allowed_origins` when a host composes its own stack."
    },
    {
      "name": "stapel_realtime.testing",
      "kind": "test_harness",
      "summary": "open_stream()/StreamClient — the envelope-aware Channels test client a module uses to test its own consumer, instead of the fourth hand-wired WebsocketCommunicator."
    }
  ],
  "surface": [
    {
      "name": "build_websocket_application",
      "kind": "factory",
      "path": "stapel_realtime.asgi.build_websocket_application",
      "intent": "Assemble the host's whole ProtocolTypeRouter in one call: origin guard (compared WITH the port) over core's G14 JWT stack over a URLRouter of every installed module's routing manifest. This is what a host's asgi.py should contain instead of a hand-written stack — the three existing hand-written ones are why the fleet has three different auth and close-code conventions.",
      "instead_of": [
        "a hand-written ProtocolTypeRouter/URLRouter/AuthMiddlewareStack in a host's asgi.py"
      ]
    },
    {
      "name": "collect_websocket_urlpatterns",
      "kind": "factory",
      "path": "stapel_realtime.asgi.collect_websocket_urlpatterns",
      "intent": "Discover every installed app's `<package>.routing.websocket_urlpatterns`. Libraries carry the manifest and the assembly reads it, so a scaffold can emit an ASGI file without knowing any module by name — and a routing module that fails to import raises instead of silently contributing no routes.",
      "instead_of": [
        "a hand-kept list of module routing imports in the host"
      ]
    },
    {
      "name": "normalize_origin",
      "kind": "factory",
      "path": "stapel_realtime.asgi.normalize_origin",
      "intent": "Canonicalize an Origin header or allowlist entry to scheme://host[:port], lower-cased with the scheme's default port elided. The port is identity: an allowlist entry of 'studio.localhost' never matching 'http://studio.localhost:8600' is the incident the origin guard and realtime.E003 exist for."
    },
    {
      "name": "site_registry_origins",
      "kind": "factory",
      "path": "stapel_realtime.asgi.site_registry_origins",
      "intent": "https://<host> for every host and alias in the stapel_core.sites registry — the origins OriginGuard unions into its allowlist so one image answers every registered brand host without a hand-list per socket. Empty when the registry is empty, broken (reported by stapel_core.sites.E001, not here) or the installed core predates it. Call it when assembling an allowlist by hand; the guard and realtime.W002 already read it.",
      "instead_of": [
        "re-listing the fleet's brand hosts in STAPEL_REALTIME['ALLOWED_ORIGINS']"
      ]
    },
    {
      "name": "deny",
      "kind": "gate_function",
      "path": "stapel_realtime.authorize.deny",
      "intent": "The default authorize hook: refuse, and log which stream a consumer forgot to gate. Fail-closed is the whole design — an open default is a leak that looks like working code in every test a module writes for itself. Do not call it; know that it is what you get by not overriding authorize()."
    },
    {
      "name": "user_id_from_scope",
      "kind": "predicate",
      "path": "stapel_realtime.authorize.user_id_from_scope",
      "intent": "The connecting user's pk, or None for an anonymous scope — the one place an authorizer should read identity from, so 'authenticated' means the same thing in every module's hook."
    },
    {
      "name": "close_code_name",
      "kind": "predicate",
      "path": "stapel_realtime.close_codes.close_code_name",
      "intent": "Machine name for a close code (4403 -> 'forbidden'). Use it in logs and in a client's reconnect switch instead of comparing bare numbers; TERMINAL_CLOSE_CODES next to it names the ones a client must not retry with the same credentials."
    },
    {
      "name": "deliver",
      "kind": "factory",
      "path": "stapel_realtime.delivery.deliver",
      "intent": "The transport callable the core's STAPEL_COMM['SIGNAL_TRANSPORT'] = 'channels' resolves to: transport(stream_key, frame), called after commit with the complete envelope stapel_core.comm.signal() built, forwarded to subscribers verbatim. At-most-once and best-effort — no layer, no subscriber or a dead redis all mean the frame is dropped and nothing raises. Emit with comm.signal(); call this directly only when wiring a transport by hand.",
      "instead_of": [
        "a hand-rolled async_to_sync(get_channel_layer().group_send) in a module"
      ]
    },
    {
      "name": "deliver_frame",
      "kind": "factory",
      "path": "stapel_realtime.delivery.deliver_frame",
      "intent": "Fan out one JOURNAL frame (carrying the persisted row's seq) to a resumable stream, after the row is committed. Use this — not deliver() — whenever the frame corresponds to a row a client could replay: the seq is what lets ResumableStreamConsumer deduplicate the replay/live overlap.",
      "instead_of": [
        "a module's own broadcast_message() over the channel layer"
      ]
    },
    {
      "name": "register_transport",
      "kind": "factory",
      "path": "stapel_realtime.delivery.register_transport",
      "intent": "Register deliver() into the core's signal seam under the name 'channels'. Called from this package's AppConfig.ready(); a host that composes app config by hand and wants the axis to resolve calls it itself. Registration is not activation — the host still selects the transport, and the default stays 'none'."
    },
    {
      "name": "revoke",
      "kind": "gate_function",
      "path": "stapel_realtime.delivery.revoke",
      "intent": "Kick one user (or everyone) off a stream immediately when their access ends — call it from the module's @on_action subscriber on the membership Action. The subscriber gets a `kick` frame and close 4410. This is the answer to the main leak vector: a socket that keeps streaming after the user was removed. Without it the residual window is the 30-second capability cache.",
      "instead_of": [
        "waiting for the client to reconnect before re-checking access"
      ]
    },
    {
      "name": "error_frame",
      "kind": "factory",
      "path": "stapel_realtime.envelope.error_frame",
      "intent": "Build the `error` envelope — a machine `code` (bad_envelope / bad_type / unauthorized) plus a human `message`. Refusals only; the resume-gap verdict is a `resync` FRAME, not an error, because re-hydrating is a normal instruction and the socket stays open."
    },
    {
      "name": "frame",
      "kind": "factory",
      "path": "stapel_realtime.envelope.frame",
      "intent": "Build a v1 wire envelope {v, type, payload, seq?, stream?}. Every frame on a Stapel socket goes through this, so the version and the optional-field discipline (no seq on ephemeral frames) hold everywhere.",
      "instead_of": [
        "a hand-written dict literal sent with send_json"
      ]
    },
    {
      "name": "parse_frame",
      "kind": "factory",
      "path": "stapel_realtime.envelope.parse_frame",
      "intent": "Validate an inbound envelope into a Frame dataclass, raising InvalidEnvelope rather than returning a half-trusted dict. Note the asymmetry it encodes: an unknown frame TYPE parses (the consumer answers bad_type), an unknown envelope VERSION does not (the shape underneath is unknown)."
    },
    {
      "name": "group_name",
      "kind": "factory",
      "path": "stapel_realtime.streams.group_name",
      "intent": "Translate a stream key into its Channels group name (':' is not a legal group character). Long keys fold into a digest rather than being truncated, because truncation would make two streams share one group. Only a host writing its own transport needs this; deliver()/revoke() call it for you."
    },
    {
      "name": "parse_stream_key",
      "kind": "factory",
      "path": "stapel_realtime.streams.parse_stream_key",
      "intent": "Parse a canonical key into its module/scope_type/scope_id/topic parts, raising InvalidStreamKey on anything else. This is how an authorizer learns which workspace it is being asked about — a consumer that cannot parse its own stream key must close the socket, not guess a scope."
    },
    {
      "name": "workspace_stream",
      "kind": "factory",
      "path": "stapel_realtime.streams.workspace_stream",
      "intent": "Shorthand for the common shape, <module>:ws:<workspace_id>[:<topic>] — the key form the WorkspaceCapability authorizer understands."
    },
    {
      "name": "open_stream",
      "kind": "factory",
      "path": "stapel_realtime.testing.open_stream",
      "intent": "Connect a test socket to a consumer with an already-authenticated scope, returning a StreamClient that speaks envelopes and swallows heartbeat noise. A module testing its consumer should use this instead of wiring a WebsocketCommunicator by hand — with expect_accept=False it is also how a fail-closed authorize is asserted.",
      "instead_of": [
        "a hand-wired channels.testing.WebsocketCommunicator with a hand-built scope"
      ]
    }
  ],
  "operations_total": 0,
  "requires": [
    {
      "module": "stapel-core",
      "optional": false,
      "reason": "AppSettings config layer (conf.py), the G14 Channels JWT middleware reused by build_websocket_application and its close-code constant (asgi.py, close_codes.py), and require_capability for the workspace authorizer (authorize.py). The non-fleet dependencies are extras: `channels` to SERVE a socket (the envelope, stream keys and the delivery seam work without it — delivery degrades to a silent no-op, which is the contract) and `channels-redis` for cross-replica fan-out (more than one worker without it is realtime.E001)."
    }
  ]
}
